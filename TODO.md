# MENKO ENDURANCE TODO

## アニメーション挙動確認ラボ

本編とは別ファイルの `animation_lab.html` を作成する。

### 目的

- メンコのアニメーションに感じるコンマ数秒のずれを検証する
- 判定結果と見た目の挙動を一致させる
- 本編の戦闘バランスへ影響を与えずに調整値を比較する

### 実装候補

- 現行挙動と調整後の挙動を左右で同時比較
- スロー再生、一時停止、1フレーム送り
- 「弱すぎる」から「強すぎる」まで投擲ゾーンを固定
- めくれ成功／失敗を固定
- 同じ配置、投擲位置、乱数条件で繰り返し再生
- 浮く高さ、移動距離、回転量、滞空時間を調整
- 上層から下層へ衝撃が伝わるタイミングを調整
- 衝撃発生から各メンコが動き始めるまでの時間を表示
- 60Hz／120Hzなど、描画速度による差を検証
- 確定した設定値を本編へ移植できる形式で出力

### 現時点で疑われる原因

- アニメーション進行が実経過時間ではなく `1/60` 固定になっている
- 上層のメンコが完全に着地してから下層へ衝撃を渡しているため、約0.34～0.82秒遅れて反応する場合がある

### 調整方針

- `requestAnimationFrame` の実経過時間を使う
- 上層のメンコが十分に浮いた時点で下層へ衝撃を伝える
- 「バー位置 → 動きの特徴 → 判定結果」が視覚的に一致するようにする
- 通常メンコの確率調整とは分離し、まずアニメーションだけを検証する
